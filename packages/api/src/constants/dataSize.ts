const BYTE = 1

const KB = BYTE * 1000
const KiB = BYTE * 1024

const MB = KB * 1000
const MiB = KB * 1024

const GB = MB * 1000
const GiB = MB * 1024

export const DATA_SIZE_IN_BYTES = {
    BYTE,
    KB,
    KiB,
    MB,
    MiB,
    GB,
    GiB,
}
