import { createTheme } from '@mui/material'

export const primaryTheme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#3dbaff',
        },
        secondary: {
            main: '#ff813d',
        },
    },
})
